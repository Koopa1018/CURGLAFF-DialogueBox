namespace Clouds.UI.TextWriting
{
	public interface IMessageCommand {
		//Don't know what to put here, since the character processing is to be designed still.

		//IMessageCommand FromEscapeCodeParams (string escapeCodeParams);
	}
}